# install packages
install.packages('shiny')
library(shiny)

# skeleton of a shiny app


# ui --------------------------------------------------


# server ----------------------------------------------

shinyApp(ui = ui, server = server)

